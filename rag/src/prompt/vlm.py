"""Prompts for Vision Language Model (VLM) functionality."""

# Prompt for extracting text from images using VLM
# VLM_IMAGE_EXTRACTION_PROMPT = ("你是一个专业的文档内容分析助手。请基于图片中的可见内容完成以下任务：1. 提取图片中的关键文字内容，包括：- 标题、副标题- 正文中的关键信息- 重要数据和结论性描述；2. 如果图片中包含【表格】：- 请识别表头和主要行列- 以结构化、简洁的方式概括表格所表达的数据含义3. 如果图片中包含【曲线图 / 折线图 / 趋势图/直方图】：-"
                               # " 请基于视觉信息描述其主要趋势（如：上升、下降、波动、稳定）- 指出是否存在明显的高点、低点或阶段性变化- 如果有多条曲线，描述它们之间的相对关系- 不要猜测未明确显示的数据请保持输出简洁、准确，只总结图片中明确表达的信息，避免主观推断。")
VLM_IMAGE_EXTRACTION_PROMPT = """你是一名证券研究分析师助理，负责将研究报告页面
转写为“可用于检索与问答的知识文本”。

请阅读图片中的全部可见内容，并严格按照以下顺序执行任务。

================ 执行顺序（非常重要） ================

第一步：尝试识别并抽取页面中的正文分析内容
- 包括业务描述、行业分析、经营情况、财务表现、结论性判断
- 若页面存在章节标题（如“2.1 xxx”），请将其自然融入正文语义中

第二步：在不影响正文的前提下，判断页面中的图表或表格
- 仅当图表或表格提供了正文之外的新增信息时，才进行补充性总结
- 若无法判断图表具体信息，则直接忽略该图表，不影响正文输出

第三步：仅在以下情况下才返回空字符串
- 页面仅包含封面、目录、页眉页脚、免责声明或装饰性内容
- 页面不包含任何可被复述的业务、行业、财务或结论性信息

================ 正文抽取规则 ================

- 正文抽取优先级高于所有判断
- 不要因为图表难以理解而放弃正文
- 保留原有分析逻辑，可进行轻度书面化改写
- 不输出作者、分析师、机构、联系方式、免责声明

================ 图表与表格处理规则 ================

- 不描述图表的视觉外观
- 转写为结论性、趋势性或对比性的自然语言，如果标题也是结论性观点，将标题内容也保留
- 保留关键指标名称、时间、数值和变化方向
- 不要仅输出“图表xx：xxx”这样的标题

================ 输出格式要求 ================

- 使用自然段输出，不使用列表
- 每个自然段只表达一个明确语义
- 不添加图片中不存在的信息
- 输出内容应适合直接用于文本切分与向量化

================ 重要补充说明 ================

- 即使页面信息较少，只要存在明确事实或判断，也应尽量提取
- 若正文和图表内容重复，仅保留正文表达
- 最终输出允许为空字符串，但不得随意返回空"""
# VLM_IMAGE_EXTRACTION_PROMPT = """
# 你是一个用于文档理解与信息抽取的视觉语言模型。
#
# 请仔细阅读图片中的全部可见内容，并【仅提取对理解文档核心内容有价值的信息】。
# 重点关注正文、表格和图表所表达的事实、观点和结论。
#
# ================ 提取规则 =================
#
# 【一、需要提取的内容】
# 1. 文档或页面的核心标题与章节标题
# 2. 正文中的关键观点、结论、事实性描述
# 3. 表格：
#    - 提取表格所传达的核心信息
#    - 使用简要、结构化的自然语言描述（如“字段：含义/数值”）
# 4. 图表（折线图、柱状图、饼图等）：
#    - 说明图表主题
#    - 描述主要趋势、对比关系和关键数值（如有）
#
# 【二、必须忽略的内容（不要输出）】
# - 作者姓名、分析师信息、署名
# - 证书编号、邮箱、电话、地址、邮编
# - 公司 Logo、水印、页眉、页脚
# - 免责声明、版权声明、背景装饰文字
# - 与正文含义无关的说明性文本
# - 页面目录
#
# 【三、输出格式要求（非常重要）】
# - 使用自然段输出，不要使用列表符号
# - 每个自然段只表达一个清晰、完整的语义点
# - 不要在同一段中混合多个主题
# - 不重复、不总结、不添加图片中不存在的信息
# - 语言保持客观、中性、信息密集
# - 如果内容包含数值，提取后保留文本中原有的数值
# - 对于文档等内容，适当保留一些原文的表达方式
#
# 【四、段落长度建议】
# - 单个自然段控制在 150–400 字以内
# - 如果信息很多，请拆分为多个自然段，并用空行分隔
#
# 最终输出应精简、清晰，适合直接用于后续文本切分与向量化处理。
# """
# VLM_IMAGE_EXTRACTION_PROMPT = """
# 你是一个用于文档理解与信息抽取的视觉语言模型。
#
# 请阅读图片中的全部可见内容，但【仅在页面包含正文段落、表格或图表时】才进行信息提取。
# 如果页面仅包含封面信息、标题页信息或元数据，请不要输出任何内容。
#
# ================ 提取规则 =================
#
# 【一、允许提取的前提（非常重要）】
# 仅当页面中明确存在以下至少一项时，才允许输出内容：
# - 连续的正文段落
# - 表格
# - 图表
# - 明确的结论性语句或事实性描述（即使仅有一两句话）
#
#
# 如果页面信息较少，请尽量提取其中可识别的事实性文字或结构信息
#
#
# 【二、需要提取的内容】
# 1. 正文中的内容事实性描述
# 2. 表格：
#    - 提取表格所传达的核心信息
#    - 使用简要、结构化的自然语言描述
# 3. 图表：
#    - 仅当图表中实际呈现了信息内容时才允许提取
#    - 必须至少包含以下之一：
#     · 明确的数值、比例或区间
#     · 至少两个对象之间的对比关系
#     · 趋势性变化（增长、下降、变化）
#     · 图表下方或正文中对该图表的解释性文字
#    -如果页面中出现图表标题（如“图表xx：xxx”），请不要仅输出标题，必须同时描述该图表所表达的主要内容、结构、趋势或结论，如果无法判断图表内容，则不要输出该图表标题。
#
# 【三、必须忽略的内容（不要输出）】
# - 报告封面信息、扉页信息、标题页信息
# - 作者姓名、分析师信息、署名
# - 证书编号、邮箱、电话等个人或机构联系方式
# - 公司 Logo、水印、页眉、页脚
# - 免责声明、版权声明、背景装饰文字
#
# 【四、输出格式要求】
# - 使用自然段输出，不使用列表符号
# - 每个自然段仅表达一个明确语义点
# - 不添加图片中不存在的信息
# - 若页面不满足提取前提，输出为空（不返回任何文字）
# - 对于正文段落文本内容，保留原有文档的表达方式
# - 提取出的图表主题和内容一定在同一个段落
# - 如果图表内容不需要提取，那么关于这个图表的任何内容都不需要返回，包括图表标题，比如图表xx:xxx等,直接返回空。
# - 对于包含关键数值结论的页面，请尽量使用完整自然语言句式呈现，而非仅罗列数字
#
# 【五、事实增强】
# - 若页面中包含成立时间、年份、金额、比例、次数、数量等事实，请完整保留原始表述
# - 对于财务指标或业务数据，请尽量连同指标名称一起输出
#
# 【六、轻度语义增强】
# - 在不改变事实含义的前提下，可使用常见的书面表达对原文进行轻微改写，以提高可理解性
#
# 最终输出应仅包含正文层面的信息，适合直接用于后续文本切分与向量化处理。"""

# VLM_IMAGE_EXTRACTION_PROMPT = """你是一个用于文档理解与信息抽取的视觉语言模型。
#
# 请阅读图片中的全部可见内容，并尽量提取其中可用于理解文档的文字与信息。
#
# ================ 提取规则 =================
#
# 【需要提取的内容】
# 1. 正文中的事实性描述（包括只有一两句话的说明）
# 2. 表格：
#    - 表头、字段、指标、数值
#    - 或对表格结构和含义的自然语言描述
# 3. 图表：
#    - 图表主题、结构、分类、对比或趋势
#    - 即使未包含明确数值，只要体现关系或结构，也需要描述
#
# 【可以忽略但不强制】
# - 明显重复的页眉页脚
# - 纯装饰性水印或背景文字
# - 作者姓名、分析师信息、署名
# - 证书编号、邮箱、电话等个人或机构联系方式
# - 公司 Logo、水印、页眉、页脚
# - 免责声明、版权声明、背景装饰文字
# - 与正文含义无关的说明性文本
#
# 【输出格式要求】
# - 使用自然段输出
# - 允许很短的段落
# - 不添加图片中不存在的信息
# - 保留原文关键词、术语和数值
#
# 【事实增强】
# - 若包含成立时间、金额、比例、年份等事实，请完整保留
#
# 【轻度语义增强】
# - 可在不改变事实含义的前提下进行轻微书面化表达
#
# 最终输出用于后续切分与向量化，以提高检索召回率。"""
